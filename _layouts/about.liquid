---
layout: default
---
<div class="post">
  <header class="post-header">
    <h1 class="post-title">
      {% if site.title == 'blank' %}
        <span class="font-weight-bold">{{ site.first_name }}</span> {{ site.middle_name }}
        {{ site.last_name }}
      {% else %}
        {{ site.title }}
      {% endif %}
    </h1>
    <p class="desc">{{ page.subtitle }}</p>
  </header>

  <article>
    {% if page.profile %}
      <div class="profile float-{% if page.profile.align == 'left' %}left{% else %}right{% endif %}">
        {% if page.profile.image %}
          {% assign profile_image_path = page.profile.image | prepend: 'assets/img/' %}
          {% if page.profile.image_circular %}
            {% assign profile_image_class = 'img-fluid z-depth-1 rounded-circle' %}
          {% else %}
            {% assign profile_image_class = 'img-fluid z-depth-1
      rounded' %}
          {% endif %}
          {% capture sizes %}(min-width: {{site.max_width}}) {{ site.max_width | minus: 30 | times: 0.3}}px, (min-width: 576px)
      30vw, 95vw"{% endcapture %}
          {%
            include figure.liquid loading="eager" path=profile_image_path class=profile_image_class sizes=sizes alt=page.profile.image
            cache_bust=true
          %}
        {% endif %}
        {% if page.profile.more_info %}
          <div class="more-info">{{ page.profile.more_info }}</div>
        {% endif %}
        <div
          class="google-scholar-metrics mt-4 p-3 border rounded shadow-sm"
          style="background-color: var(--global-code-bg-color); border-color: var(--global-divider-color) !important;"
        >
          <h6 class="mb-3" style="color: var(--global-theme-color); font-weight: 600;">
            <i class="ai ai-google-scholar mr-2"></i>Google Scholar Metrics
          </h6>
          <div class="d-flex justify-content-between mb-2">
            <span><i class="fas fa-graduation-cap mr-2"></i>h-index:</span>
            <span class="font-weight-bold" style="color: var(--global-theme-color);">
              {% if site.data.scholar_metrics.h_index %}{{ site.data.scholar_metrics.h_index }}{% else %}5{% endif %}
            </span>
          </div>
          <div class="d-flex justify-content-between mb-3">
            <span><i class="fas fa-quote-right mr-2"></i>Citations:</span>
            <span class="font-weight-bold" style="color: var(--global-theme-color);">
              {% if site.data.scholar_metrics.citations %}{{ site.data.scholar_metrics.citations }}{% else %}50{% endif %}
            </span>
          </div>
          <a
            href="https://scholar.google.com/citations?user={{ site.scholar_userid }}"
            target="_blank"
            rel="noopener noreferrer"
            class="btn btn-sm btn-outline-orange w-100 py-2"
            style="border-radius: 8px; font-weight: 600; text-transform: none;"
          >
            <i class="ai ai-google-scholar mr-1"></i>View Full Profile
          </a>
        </div>
      </div>
    {% endif %}

    <div class="clearfix">{{ content }}</div>

    <!-- News -->
    {% if page.news and site.announcements.enabled %}
      <h2>
        <a href="{{ '/news/' | relative_url }}" style="color: inherit">News</a>
      </h2>
      {% include news.liquid limit=true %}
    {% endif %}

    <!-- Latest posts -->
    {% if site.latest_posts.enabled %}
      <h2>
        <a href="{{ '/blog/' | relative_url }}" style="color: inherit">Latest posts</a>
      </h2>
      {% include latest_posts.liquid %}
    {% endif %}

    <!-- Selected papers -->
    {% if page.selected_papers %}
      <h2>
        <a href="{{ '/publications/' | relative_url }}" style="color: inherit">selected publications</a>
      </h2>
      {% include selected_papers.liquid %}
    {% endif %}

    <!-- Social -->
    {% if page.social %}
      <div class="social">
        <div class="contact-icons">{% include social.liquid %}</div>

        <div class="contact-note">{{ site.contact_note }}</div>
      </div>
    {% endif %}
    <div style="width: 300px; margin: 0 auto">
      <script
        async
        type="text/javascript"
        id="mapmyvisitors"
        src="//mapmyvisitors.com/map.js?d=uvhJTZhmhmsmEpNBdRidHFtoSP7wyIXgd-PsHdpHeAo&cl=ffffff&w=a"
      ></script>
    </div>
    <br>
    <div align="center">
      <a href="https://me-qr.com" border="0" style="cursor:pointer;display:block">
        <img src="https://cdn2.me-qr.com/qr/118559390.png?v=1721720379" alt="Create qr code for free" width="300" height="300">
      </a>
      <a href="https://me-qr.com" border="0" style="cursor:default;display:none">Create qr code for free</a>
    </div>
  </article>
</div>
